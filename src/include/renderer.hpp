#include <glfwHandler.hpp>
#include <board.hpp>
#include <shader.hpp>
#include <gameState.hpp>

namespace Renderer{
  void render(glfwHandler &hwnd, Board &board, Shader &shader, GameState &gameState);
};
